# 🧑 00：M1 の着手前に決めること

> [`README.md`](./README.md)（M1 の概要）を読んだうえで、Issue を切り出す前に所有者が判断する問いである。
> 各問いに**おすすめ**を付記した。「おすすめで」と合意すれば、すべておすすめの方針で進められる。
> 方針が決まったら、下表の「決定」列に決定日とともに記録する。
> 状態：**M1a の問いはすべて決定**（2026-09-15。同日、M1 を M1a と M1b に分けたのに合わせて見直し、所有者が回答した）。
> **2026-09-16、M1.1 の Issue を切る前に Q14〜Q17 を足し、所有者がすべておすすめで決めた。**

**見直しで変わったこと**：進め方を「手書きのサンプルアプリ先行」に変え、M1 を M1a（見本 3 つと契約）と M1b（CommandAgent との結合）に分けた（[`01-integration-strategy.md`](./01-integration-strategy.md) §5・§6）。

- Q1・Q2 は、ゲートの宣言（`01` §5.3）で**解決した**
- Q3・Q6・Q9・Q10 は、**中身を M1a に合わせて書き直した**
- Q7・Q8 は **M1b の問い**になった。今は答えなくてよい
- 見本を書いて見つかった決めごとを **Q13 として足した**

**回答（2026-09-15）**：M1a の問いのうち、所有者が答えるべき問いを Q4・Q10・Q13 に絞り、所有者が回答した。**残りの M1a の問い（Q3・Q5・Q6・Q9・Q11・Q12）は、おすすめで進める。**

---

## 早見表

### M1a の着手前に答えてほしい問い

| # | 問い | おすすめ | 決定 |
|---|---|---|---|
| Q3 | 宣言の形だけでは決まらない「実行時の意味」を、誰がどこに持つか | MUSUNEST が見本を動かしながら決め、スキーマの新しい版と一緒に `appspec-schema` に文書で持つ | おすすめ（2026-09-15） |
| Q4 | 「Form A の経路」を M1 でどこまで作るか（テンプレ #38 の扱い） | L2（宣言だけ）の経路に絞る。テンプレの中身と tarball（#38）は M2 へ移す | **テンプレ（#38）は M2 へ移さず、M1a で作る**。経路は L2 に絞る（2026-09-15 所有者） |
| Q5 | ログインが無い間、アプリの API をどこで有効にするか | dev・staging だけ。production では 404 | おすすめ（2026-09-15） |
| Q6 | 宣言をどう置くか（R2 に置いて登録する経路） | 手元のスクリプト（publish）で、静的チェックを通してから置く。M1b の納品物も同じ publish に渡す | おすすめ（2026-09-15） |
| Q9 | ピン差し替えの儀式の対象は何か | スキーマのピンを、v0.1（工場の仮置き）から、このリポジトリの新しい版（`community.app-spec/v0.2`）へ差し替える | おすすめ（2026-09-15） |
| Q10 | M1a の着手日と期限 | M1a の Issue を起票した日を着手日にし、その日に Issue の量を見て期限を宣言する | **期限は設けない**。着手日（M1a の Issue を起票した日）と完了日は記録する（2026-09-15 所有者） |
| Q11 | リアルタイム同期（DO の WebSocket）を M1 に入れるか | 入れない（M2）。M1 は再読み込みで同じデータが見えれば足りる | おすすめ（2026-09-15） |
| Q12 | 宣言を Worker でどう持つか（CPU 10 ms 対策） | R2 に原本（YAML）を置き、publish の時点で静的チェック済み・正規化済みの形（JSON）も作って置く。CPU は staging で実測する | おすすめ（2026-09-15） |
| Q13 | 利用者の画面の数字に出る、計算の決めごと | 端数は 1 円未満を切り捨てて余りは払った人が持つ／精算は金額の大きい順に組む／日付は日本時間／参照されているものは消せない／空は 0／同じ値は登録順 | **おすすめ**（2026-09-15 所有者） |

### M1.1 の前に足した問い（2026-09-16）

README を roadmap・`04`・M0 の清算・コードと突き合わせて見つかった、所有者が決めること。

| # | 問い | おすすめ | 決定 |
|---|---|---|---|
| Q14 | 依存の向きを、M1.1 に合わせて変えるか | `data-api → control-plane`・`control-plane → spec-engine`・`e2e → sdk` を許可する。publish の中身は control-plane に置き、`infra/scripts` は薄い呼び出しだけにする | おすすめ（2026-09-16 所有者） |
| Q15 | `/api` の経路の CPU 時間の判定の線を、測る前に宣言するか | 落ち着いた状態で、Worker 単体の最大が 7 ms を超えたら（余裕 3 ms 未満）Workers Paid へ上げる | おすすめ（2026-09-16 所有者） |
| Q16 | staging の e2e をどこで回すか | `deploy-staging` の貫通スモークのあとに、CI で回す（検証ゲートにはしない） | おすすめ（2026-09-16 所有者） |
| Q17 | 時刻で変わる値の採点で、時計をどう固定するか | 時計を差し込んだテスト（vitest）で採点する。staging の e2e は、時計に依存しない値と SHA の照合だけにする | おすすめ（2026-09-16 所有者） |
| Q18 | M1.1・M1.2 の契約の決めごと（式の上限・空 list・集計の null・settle の形・余りの配賦・整数円・並べ直し・update） | 下の Q18 の表のとおり | **おすすめ**（2026-09-16 所有者） |
| Q19 | 式の文法を、どの粒度で書き、どこに置くか（#168） | 構造は JSON Schema・式は EBNF・規則は表で、1 つの版付きの契約に束ねる | **おすすめ**（2026-09-24 所有者。置き場所は `packages/appspec-schema/contract/`） |

### M1b の問い（今は答えなくてよい）

| # | 問い | おすすめ | 決定 |
|---|---|---|---|
| Q7 | L2 の納品物の `assurance: partial` を受け入れるか | 受け入れる。`verdict: full` を必須にし、実行時のスモークは MUSUNEST の e2e で埋める | |
| Q8 | 決定的な再検証をどこで実行するか | 所有者の手元（ピンした CommandAgent のバイナリの SHA を照合してから） | |

### 解決した問い

| # | 問い | どう決まったか | 決定 |
|---|---|---|---|
| Q1 | ゲートで使うアプリはどれか | M1a は手書きの見本 3 つ、M1b は工場が新しい契約で作った 3 つのアプリ | 2026-09-15（ゲートの宣言） |
| Q2 | ゲートの「期待値」を何とするか | 見本ごとの採点のシナリオ（`02` の §2.3・§3.3・§4.4）。精算も入る | 2026-09-15（ゲートの宣言） |

---

## Q1. ゲートで使うアプリはどれか（解決）

- **M1a**：手書きの見本 3 つ（割り勘・タスク管理・ダッシュボード）で判定する
- **M1b**：工場（CommandAgent）が新しい契約で作った 3 つのアプリで判定する
- 以前のおすすめだった封緘済み bundle（`cm4-delivery-bundle-001`。スキーマ v0.1）は、ゲートには使わない。v0.1 と新しい版の違いを確かめる記録として、README §5.2 に残した

---

## Q2. ゲートの「期待値」を何とするか（解決）

- 見本ごとの採点のシナリオ（[`02-l2-spec-examples.md`](./02-l2-spec-examples.md) の §2.3・§3.3・§4.4）の値を期待値にする
- 割り勘の精算（「C さん → A さん 3,000 円」）も入る（`01` の D4）
- Q13 の決めごとによって期待値が変わる場合は、ゲートの変更として所有者が `01` §5.3 に日付とともに記録する。**Q13 のおすすめなら、今の採点のシナリオの値は変わらない**

---

## Q3. 宣言の形だけでは決まらない「実行時の意味」を、誰がどこに持つか

### 何を決めるか
スキーマ（宣言の形）だけでは答えが 1 つに決まらない振る舞いを、誰が決め、どこに書いておくかを決める。例：`list` の中身、画面の部品の描き方、操作の動き、端数や「今日」の数え方（`02` §6.1）。

### なぜ今決める必要があるか
画面（Instant Renderer）・API（data-api）・保存（DO）・静的チェックのどれも、この意味が決まらないと作れないため。

### 選択肢
| 選択肢 | 良い点（メリット） | 気になる点（デメリット・課題） |
|---|---|---|
| **A. MUSUNEST が見本を動かしながら決め、スキーマの新しい版と一緒に `appspec-schema` に文書で持つ**（おすすめ） | 店頭が実際に動かせる意味だけが契約に入る。スキーマと意味を同じ版で管理できる | 工場の検査の前提とずれる恐れがある（段階 2 で工場側の契約文書と照らしてから固める） |
| B. v0.1 のバイト列を変えずに、v0.1 の意味だけを決める（以前のおすすめ） | スキーマを差し替えずに済む | 見本 3 つは v0.1 では書けないので、成り立たない |
| C. 工場側（CommandAgent）に決めてもらう | 工場の検査とずれない | スキーマの正本はプラットフォームが持つという契約と逆になる。工場側の裁定を待つ間、M1a が止まる |

### おすすめと理由
- **おすすめ**：A
- **理由**：契約を先に決めるのは店頭の仕事であり（`01` §2.1）、見本を動かしながら決めれば、机上で決めた意味が動かないという手戻りが無いため
- **利用者の画面の数字に出る決めごと**は、採点の期待値に効くので、着手前に Q13 で決める。それ以外（画面の部品の描き方など）は、M1a の中で決めて文書に書く

### 決定（2026-09-15）
- **おすすめ（A）で進める**（所有者が答える問いを Q4・Q10・Q13 に絞り、残りはおすすめで進めることにした）

---

## Q4. 「Form A の経路」を M1 でどこまで作るか（テンプレ #38 の扱い）

### 何を決めるか
企画書にある「Form A の経路（共通ホスト＋アドオンの動的読み込み）」をどこまで作るか、およびテンプレート tarball の SHA を固定するタスク（#38）の扱いを決める。

### なぜ今決める必要があるか
M1 の範囲を L2（宣言だけ）に絞るか、L3/L4（アドオンのコードの実行・サンドボックス）まで広げるかで、設計と作業量が大きく変わるため。

### 選択肢
| 選択肢 | 良い点（メリット） | 気になる点（デメリット・課題） |
|---|---|---|
| **A. L2（宣言だけ）の経路に絞り、テンプレ（#38）は M2 へ移す**（おすすめ） | R2 の宣言 → data-api → host（Instant Renderer）→ DO の経路に集中できる | 企画書の「実テンプレの確定・Form A 統合スモーク」のうち、テンプレ側の検証は M1 で終わらない |
| B. L3/L4 のアドオンの動的読み込みまで作る | 将来の拡張経路（esbuild で束ねた `app-zone/` のコードの動的実行）を先に確かめられる | 見本 3 つはどれも L2 で、アドオンのコードが無いので確かめられない。L4 のサンドボックス設計なども要り、M1a に収まらない |

### おすすめと理由
- **おすすめ**：A
- **理由**：見本 3 つと MVP のアプリに要るのは、L2 の経路だけであるため。テンプレの構成は、実物の Template App を作る M2 で要件とともに決めるほうが手戻りが少ない

### 決定（2026-09-15 所有者）
- **テンプレ（#38）は M2 へ移さない。** M1 に残し、**M1a で作る**（テンプレはスキーマと同じく、店頭が正本を持つ契約（企画書 13 章 Template as Contract）なので、「このリポジトリで完結する」M1a に入る）
- M1a で作るもの（#38 の完了条件のとおり）：
  - `templates/tanstack-start` の中身（工場が L3/L4 のコードを置く `app-zone/` と、変えてはいけない `core/`・使ってよい `sdk/` の境界）。host と同じく **SSR にしない**
  - 同じ入力から同じバイト列の tarball を作るコマンド
  - 配布形態（GitHub Releases のアセットか R2 か）の決定（🧑🤖）
  - `pins/commandagent.json` の `template_tarball` の実値
- **経路は L2 に絞るのは変えない。** 見本はすべて L2 なので、テンプレの `app-zone/` に置かれるコードを店頭で動かすこと（アドオンの動的読み込み）は M1 では作らない。**そこまで M1 に入れる場合は、改めて決める**
- M1b：工場（CommandAgent）側で、測定用の合成テンプレ（`synthetic-community/`）を本物のテンプレに差し替える（CommandAgent の契約文書 §4 の手順）

---

## Q5. ログインが無い間、アプリの API をどこで有効にするか

### 何を決めるか
認証（Google OAuth）が無い M1 の間、ミニアプリの API をどの環境で有効にするかを決める（[`README.md`](./README.md) §7.3）。

### なぜ今決める必要があるか
今の CD は、main へのマージで staging へ、v タグで production へ自動でデプロイする。認証の無い書き込み可能な API が production に出ないように、先に決めておく必要があるため。

### 選択肢
| 選択肢 | 良い点（メリット） | 気になる点（デメリット・課題） |
|---|---|---|
| **A. dev・staging だけで有効にし、production では 404 を返す**（おすすめ） | production にデータが書き込まれる危険を確実に止められる。CD を止めずに済む | 環境による分岐が 1 つ増える（`vars.ENVIRONMENT` で判定し、テストで固定する） |
| B. M1 の間は production への v タグを打たない | コードに環境の分岐を足さずに済む | セキュリティ修正など、ほかの大事な修正を production に出せなくなる |
| C. production でも API を有効にする | 全環境で同じ動きになる | URL を知っていれば、誰でも production の DO に書き込める |

### おすすめと理由
- **おすすめ**：A
- **理由**：production へのデプロイ経路を塞がずに、認証の無い API の公開を確実に防げるため

### 決定（2026-09-15）
- **おすすめ（A）で進める**（所有者が答える問いを Q4・Q10・Q13 に絞り、残りはおすすめで進めることにした）

---

## Q6. 宣言をどう置くか（R2 に置いて登録する経路）

### 何を決めるか
宣言（M1a は手書きの見本、M1b は工場の納品物から取り出したもの）を、Cloudflare（R2 と D1）に置いて登録する手段を決める。

### なぜ今決める必要があるか
M1a の最初の経路（割り勘を置いて動かす）に要るため。スクリプトの置き場所と、使う資格情報も決まる。

### 選択肢
| 選択肢 | 良い点（メリット） | 気になる点（デメリット・課題） |
|---|---|---|
| **A. 手元のスクリプト（publish）が、静的チェックを通してから R2 に置き、D1 に登録する**（おすすめ） | 管理用の API や認証を新しく作らずに済む。通らない宣言は置かれない。M1b では、納品物の manifest を照合してから同じ publish に宣言を渡せる | 開発者が手元から実行する（`.env` の staging の資格情報を使う） |
| B. data-api に管理用の API を作り、そこへ送る | 将来の自動化に向けた API を先に作れる | data-api は外部ルートを持たない規律なので、gateway に管理用の入口と認証が要り、M2 の認可より先に作ることになる |
| C. Builder Plane から Queue 経由で自動で置く | 手作業が無い | M2 の連携 Lv2 の範囲で、M1 を超える |

### おすすめと理由
- **おすすめ**：A
- **理由**：最小の構成で確実に経路を通せるため。手元のスクリプトから R2 / D1 に書き込むのは、CD が D1 マイグレーションを実行するのと同じ「運用者の操作」であり、「Data API が唯一の権限強制点」というリクエスト経路の規律には触れない
- **補足**：staging への見本の配置を CD に組み込むかは、A ができてから決める → **2026-09-16 に決めた。CI が配る**（`deploy-staging` の貫通スモークのあとに publish。Q18-9）

### 決定（2026-09-15）
- **おすすめ（A）で進める**（所有者が答える問いを Q4・Q10・Q13 に絞り、残りはおすすめで進めることにした）

---

## Q7. L2 の納品物の `assurance: partial` を受け入れるか（M1b）

### 何を決めるか
工場の結果に含まれる保証のラベル `assurance: partial` を、MUSUNEST の受け入れ基準として認めるかを決める。

### なぜ決める必要があるか
CommandAgent の headless 契約では「呼び出し側は verdict と assurance の両方で合否を判定する」と決まっているため、プラットフォーム側の受け入れの規則を書いておく必要がある。M1b の Issue を切るときに決めればよい。

### 選択肢
| 選択肢 | 良い点（メリット） | 気になる点（デメリット・課題） |
|---|---|---|
| **A. L2 は `verdict: full` を必須にし、`assurance` は `partial` 以上を受け入れる**（おすすめ） | L2 の納品物をそのまま受け入れられる。実行時のスモークの不足は MUSUNEST の e2e（採点のシナリオ）で埋める | 受け入れの規則を、成果物の水準（L2 と L3/L4）で分けて持つ必要がある |
| B. `assurance: full` を常に必須にする | すべての成果物に、工場側での画面の検証まで求められる | L2 は画面のコードを持たないので工場側で画面を動かせず、L2 の納品物が原理的に通らない |

### おすすめと理由
- **おすすめ**：A
- **理由**：L2 の `verdict: full` は「spec の検証は完了した。実行時のスモークはプラットフォーム統合側が受け持つ」という契約上の意味であるため。画面での動きは MUSUNEST の e2e で確かめる

---

## Q8. 決定的な再検証をどこで実行するか（M1b）

### 何を決めるか
工場の納品物に対する決定的な再検証（offline verifier による同一性の確認）を、どの環境で実行するかを決める。

### なぜ決める必要があるか
M1b の完了条件（連携 Lv1 の ④）を満たす実行環境と、CI への影響を決める必要があるため。M1b の Issue を切るときに決めればよい。

### 選択肢
| 選択肢 | 良い点（メリット） | 気になる点（デメリット・課題） |
|---|---|---|
| **A. 所有者の手元で実行する**（おすすめ） | CI に Rust のツールチェーンや外部のバイナリを持ち込まずに済む。ピンした CommandAgent のバイナリの SHA-256 を照合してから実行する | CI のログに自動では残らないので、手順を runbook にまとめて実行の記録を残す |
| B. GitHub Actions（CI）で実行する | PR ごとに自動で再検証され、客観的な記録が残る | ビルド時にネットワーク越しで外部のバイナリを取得しないピンの規律とぶつかり、CI に重いツールチェーンが要る |

### おすすめと理由
- **おすすめ**：A
- **理由**：CI の軽さとピンの規律を保つため。なお、manifest の照合（全ファイルの SHA-256・構成の確認）は TypeScript で作れるので、こちらは CI（e2e）で毎回自動で行う

---

## Q9. ピン差し替えの儀式の対象は何か

### 何を決めるか
M1 の完了条件にある「ピン差し替えの儀式（新旧を並べて照合 → 回帰 → 旧を外す → 1 コミットで記録）」の対象を決める。

### なぜ今決める必要があるか
進め方を変えたことで、スキーマを v0.1（工場の測定用の仮置き）から、このリポジトリの正本へ差し替える**本来の意味の差し替え**ができるようになった。M1a のゲートの判定 6（スキーマの正本の SHA-256 が `pins/` に記録されている）にも直結する。

### 選択肢
| 選択肢 | 良い点（メリット） | 気になる点（デメリット・課題） |
|---|---|---|
| **A. スキーマのピンを、v0.1 から、このリポジトリの新しい版へ差し替える。工場側の差し替えの儀式は M1b で行う**（おすすめ） | 契約の正本がプラットフォームにある形に揃う。M1a のゲートの判定 6 と一致する | M1a から M1b までの間、ピンの値（新しい版）と工場の手元（v0.1）が食い違う。食い違いを `pins/` に明記し、M1b で工場側を差し替える |
| B. 封緘済み bundle（`cm4-delivery-bundle-001`）のピンを足す（以前のおすすめ） | 実在する成果物で手順を試せる | v0.1 の生成物で、今のゲートには使わない（Q1） |
| C. CommandAgent の revision を新しい版へ差し替える | 工場本体の版を上げる手順を試せる | 2026-09-15 時点で、差し替え先の新しい revision が無い |

### おすすめと理由
- **おすすめ**：A
- **理由**：M1a で作る契約の正本を、工場へ渡す値として確定させる手順そのものであるため
- **版の名前**：`community.app-spec/v0.2` にする（工場側の版の名前の並びに合わせる）。あわせて、`packages/appspec-schema` の `APPSPEC_SCHEMA_VERSION`（今は `0.0.0-m0`）もこの名前に揃える

### 決定（2026-09-15）
- **おすすめ（A）で進める**（所有者が答える問いを Q4・Q10・Q13 に絞り、残りはおすすめで進めることにした）

---

## Q10. M1a の着手日と期限

### 何を決めるか
M1a のゲートの期限の起点になる着手日と、期限そのものをいつ決めるかを決める。

### なぜ今決める必要があるか
M1a のゲートの期限は「Issue を切り終えた時点で、Issue の量を見て宣言する（目安：着手から 4 週間）」と宣言した（`01` §5.3）。いつを起点にするかを先に決めておく必要があるため。

### 選択肢
| 選択肢 | 良い点（メリット） | 気になる点（デメリット・課題） |
|---|---|---|
| **A. M1a の問い（Q3〜Q6・Q9〜Q13）が決まり、M1a の Issue を起票した日を着手日にする。その日に Issue の量を見て期限を宣言する**（おすすめ） | 決まっていないことによる手戻りが無い状態で、作業の開始と期限の起点が一致する | すべての問いが決まるまで、着手日が決まらない |
| B. 問いが残っている段階で、日付を先に決める | 終わりの目標日が早く決まる | 判断が遅れると、その分だけ作業期間が削られる |

### おすすめと理由
- **おすすめ**：A
- **理由**：判断が済んでいない状態でタイマーを動かさないため。決めた着手日と期限は、所有者が `01` §5.3 に書き込む
- **M1b**：M1a のゲートに合格し、段階 3（工場の調整）の見通しが立った時点で、着手日と期限を決める

### 決定（2026-09-15 所有者）
- **M1a の期限は設けない。** `01` §5.3 の M1a の期限を「設けない」に直した
- 着手日（M1a の Issue を起票した日）と、ゲートに合格した日は記録する。M0 と同じく、終わったら清算し、かかった期間を次の見積もりの材料にする
- **着手日は 2026-09-16**（M1.1 の Issue #96〜#105 と M1.2 の Issue #106〜#112 を起票した日）。`01` §5.3 に記録した
- 期限の代わりに、**割り勘が staging で動いた時点の途中確認**（README §10 の M1.2 の終わり）で進み具合を見て、残りの量を見直す
- M1b の期限は、`01` §5.3 のとおり、段階 3 の見通しが立った時点で決める

---

## Q11. リアルタイム同期（DO の WebSocket）を M1 に入れるか

### 何を決めるか
Durable Object の WebSocket を使ったリアルタイムのデータ同期を、M1 の範囲に入れるかを決める。

### なぜ今決める必要があるか
画面（host）とサーバ（DO）の通信の作り方と、Cloudflare の無償枠への効き方に関わるため。

### 選択肢
| 選択肢 | 良い点（メリット） | 気になる点（デメリット・課題） |
|---|---|---|
| **A. M1 には入れず、M2 で作る**（おすすめ） | 設計と実装が大きく簡単になり、M1a の作業を画面と計算に集中できる | 複数の端末で同時に開いたとき、相手の変更を見るには再読み込みが要る |
| B. M1 にリアルタイム同期を入れる | 最初から、入力がすぐ相手の画面に出る | WebSocket の接続の維持や休止の扱いが要り、無償枠への影響の調査と作業が M1a に収まらない |

### おすすめと理由
- **おすすめ**：A
- **理由**：M1a のゲートの判定 3 は「再読み込み・別端末でもデータが残る（永続化）」であり、即時の同期までは求めていないため。まず HTTP での確実な読み書きを成立させる

### 決定（2026-09-15）
- **おすすめ（A）で進める**（所有者が答える問いを Q4・Q10・Q13 に絞り、残りはおすすめで進めることにした）

---

## Q12. 宣言を Worker でどう持つか（CPU 10 ms 対策）

### 何を決めるか
アプリの宣言（`app.spec.yaml`）を Cloudflare Workers でどう持ち、どう読んで、CPU 時間 10 ms の上限を守るかを決める。

### なぜ今決める必要があるか
リクエストのたびの YAML の解析や静的チェックは CPU を使い、無償枠の上限（10 ms）や昇格トリガー（P-1）に関わるため。見本には集計もあり、計算の量も増える。

### 選択肢
| 選択肢 | 良い点（メリット） | 気になる点（デメリット・課題） |
|---|---|---|
| **A. R2 に原本（YAML）を置き、publish の時点で静的チェック済み・正規化済みの形（JSON）も作って一緒に置く**（おすすめ） | Worker の実行時に YAML の解析と静的チェックを省けて、CPU を抑えられる。静的チェックツールがそのまま正規化も受け持てる | 原本と正規化した形の対応を管理する仕組みが要る（JSON 側に原本の SHA-256 を持たせるなど） |
| B. リクエストのたびに R2 から YAML を取得して、解析と静的チェックを行う | R2 に原本だけを置けばよく、publish が単純になる | YAML のライブラリの大きさと解析の CPU が、10 ms の上限に効く |
| C. インスタンスを作るときに、各 DO の SQLite に正規化した宣言を持たせる | 一度作れば、R2 を読まずに DO の中で速く参照できる | 宣言を更新するとき、同じアプリの多数の DO に 1 つずつ伝える必要がある |

### おすすめと理由
- **おすすめ**：A
- **理由**：10 ms に収めやすく、DO の管理も単純に保てるため。どの方式でも、staging で宣言の読み込み・計算・集計の CPU 時間を実測し、`../m0/06-plan-and-limits.md` の昇格トリガーに照らす

### 決定（2026-09-15）
- **おすすめ（A）で進める**（所有者が答える問いを Q4・Q10・Q13 に絞り、残りはおすすめで進めることにした）

---

## Q13. 利用者の画面の数字に出る、計算の決めごと

### 何を決めるか
見本を動かすときに、宣言の形だけでは答えが 1 つに決まらない計算のうち、**利用者の画面の数字や並びに出るもの**を決める（`02` §6.1）。

### なぜ今決める必要があるか
採点のシナリオの期待値と、M1a のゲートの判定 1（e2e）に直接効くため。作り始めてから変えると、ゲートの期待値が動く。

### 決めごとと、おすすめ

| 決めごと | 出てくる見本 | おすすめ | ほかの案 |
|---|---|---|---|
| 割り勘の端数 | 割り勘 | 1 人あたりは **1 円未満を切り捨て**、余りは**払った人が持つ**（払った人が割る人に入っていなければ、割る人のうち登録が一番早い人が持つ） | 小数のまま計算し、精算の金額だけ丸める ／ 余りを割る人に 1 円ずつ配る |
| 精算の組み方 | 割り勘 | 受け取る人と払う人を、それぞれ**金額の大きい順**に並べて組み合わせる（同じ金額はメンバーの登録順）。送金は多くても「人数 − 1」回 | 送金の回数が最少になる組み合わせを探す（人数が増えると計算が重く、CPU 10 ms に効く） |
| 「今日」「今月」 | タスク管理・ダッシュボード | **日本時間（Asia/Tokyo）**で数える。採点では時計を固定する | 端末の時間帯で数える（人によって「今日」が変わる） |
| 参照されているものを消すとき | 割り勘・タスク管理 | **消せないようにして、理由を出す**（例：「支出に名前が残っています」） | 一緒に消す ／ 名前だけ残して「削除済み」と出す |
| 空のとき | すべて | 合計・件数は **0**。費用が空の活動は、合計では 0 として数える。「1 回あたり」は活動が 0 回なら 0 | 空なら「—」を出す |
| 割り算の表示（平均など） | ダッシュボード | **小数第 1 位まで**（第 2 位を四捨五入） | 整数に丸める |
| 同じ値の並び | ダッシュボード | **メンバーの登録順** | 名前順（日本語の並び順が揺れる） |

### おすすめと理由
- **おすすめ**：上の表の「おすすめ」の列すべて
- **理由**：
  - 端数：割り勘アプリでよくある決め方で、合計が必ず支出の金額と一致する
  - 精算の組み方：誰が計算しても同じ結果になり、計算が軽い。最少回数の保証は無いが、少人数では差が出にくい
  - 日付：仲間うちで「今日」が揃う。利用者ごとの時間帯が要るようになったら、そのときに足す
  - 消すとき：入力済みのデータが黙って消えない
- **採点のシナリオへの影響**：おすすめどおりなら、`02` の §2.3・§3.3・§4.4 の期待値は変わらない

### 決定（2026-09-15 所有者）
- **上の表の「おすすめ」の列すべてを採用する。** 採点のシナリオの期待値は変わらない
- M1a の中でこの決めごとを変えるときは、所有者の承認を得て、ここと `01` §5.3 に記録する

---

## Q14. 依存の向きを、M1.1 に合わせて変えるか

### 何を決めるか
M1.1 で作る D1 の登録・publish・staging の e2e を、`infra/scripts/dep-graph.mjs` のどの向きで作るかを決める。

### なぜ今決める必要があるか
M1.1 の最初の Issue で、今の許可では作れないか、検査が届かない作りになるため。

- data-api は D1 の登録を読む。登録表のマイグレーションは control-plane にあるが、`data-api → control-plane` は許可されていない（許可されているのは gateway だけ）
- publish を `infra/scripts` に置くと、そこは pnpm workspace の外で、`.ts` を相対パスで import する（`infra/tsconfig.json`）。`check-deps.mjs` の検査が届かない
- e2e の許可が空（`"@musunest/e2e": []`）で、型付きクライアント（sdk）を使えない

### 選択肢
| 論点 | おすすめ | ほかの案 |
|---|---|---|
| data-api が D1 の登録表を読む | 表の定義と読み書きの関数を control-plane に置き、**`data-api → control-plane` を許可する** | data-api に SQL を直接書く。dep-graph は変えずに済むが、表の定義がマイグレーションと二重になる |
| publish の中身 | 静的チェック → 正規化 → 登録する行を作る処理を control-plane に置き、**`control-plane → spec-engine` を許可する**。`infra/scripts` には、資格情報と R2・D1 への書き込みを扱う薄い呼び出しだけを置く | 全部を `infra/scripts` に置く。依存の向きの検査が効かない |
| e2e のクライアント | **`e2e → sdk` を許可する**。host と同じクライアントで契約を確かめられる | fetch を直接書く |

### おすすめと理由
- **おすすめ**：上の表の「おすすめ」の列すべて
- **理由**：D1 を触る Worker は data-api だけで、表の定義を control-plane の 1 か所に置ける。中身を workspace のパッケージに置けば、`pnpm lint` の依存の検査が届く

### 決定（2026-09-16 所有者）
- **おすすめで進める。** `infra/scripts/dep-graph.mjs` に 3 本を足し、`CLAUDE.md` の依存の図を許可リストどおりに描き直した（#94）
- 静的チェックの入口（手元と CI から呼ぶコマンド）も同じ考え方にする。中身は spec-engine、入口は薄くする

---

## Q15. `/api` の経路の CPU 時間の判定の線を、測る前に宣言するか

### 何を決めるか
M1.2 で `/api` の経路（宣言の読み込み・計算・集計）の CPU 時間を staging で測る前に、Workers Paid へ上げる線を決める。

### なぜ今決める必要があるか
M0 の清算（`../m0/06-plan-and-limits.md` §8）では、落ち着いた状態で Worker 単体の最大が 5.20 ms（余裕 4.80 ms）、3 Worker の最大の和が 9.36 ms（合算なら余裕 0.64 ms）だった。上限を Worker ごとに判定するか合算するかは未確定である（同 §7 #1）。
`/api` の経路には宣言の読み込みと集計が乗るので、どちらの解釈を取るかで結論が変わる。M0 では「余裕 3 ms」の線を測ったあとで解釈を決めることになったので、今回は測る前に決めておく。

### 選択肢
| 選択肢 | 良い点（メリット） | 気になる点（デメリット・課題） |
|---|---|---|
| **A. 落ち着いた状態で、Worker 単体の最大が 7 ms を超えたら（余裕 3 ms 未満）上げる**（おすすめ） | M0 の「余裕 3 ms」の線を、実測できる単位（Worker ごとの cpuTime）で宣言し直せる | 合算で判定されていた場合、エラーが出るまで気づかない（P-1 で拾う） |
| B. P-1（CPU 超過のエラーが 7 日で 1 件）だけを線にする | 実害が出たときだけ上げるので、費用を抑えられる | エラーが出るまで上げない。利用者の画面が壊れてから動くことになる |
| C. 3 Worker の最大の和で判定する | 合算の解釈でも安全 | `/healthz` の時点で余裕が 0.64 ms なので、測ってすぐ昇格になる見込み |

### おすすめと理由
- **おすすめ**：A
- **理由**：判定の単位が実測値と揃い、エラーを待たずに上げられるため。合算の場合の取りこぼしは P-1 で拾う

### 決定（2026-09-16 所有者）
- **おすすめ（A）で進める。** `../m0/06-plan-and-limits.md` §5 に **P-7** として足した。触れたら議論せず上げる
- 測り方は §8 の再測と同じ（先に 5 回温め、60 秒空けてから 20 回）。見本ごとに、データを増やしたときも測る。3 Worker の最大の和は記録するが、判定には使わない

---

## Q16. staging の e2e をどこで回すか

### 何を決めるか
staging に置いた見本に対する e2e（staging の見本の SHA-256 の照合、時計に依存しない採点の値）を、どこで・いつ回すかを決める。

### なぜ今決める必要があるか
M1.2 で採点のシナリオの e2e を作るため。`04` §2 では、検証ゲート（`.commandmate/verify.yaml`）は増やさないと決めている。

### 選択肢
| 選択肢 | 良い点（メリット） | 気になる点（デメリット・課題） |
|---|---|---|
| **A. `deploy-staging` の貫通スモークのあとに、CI で回す**（おすすめ） | main へのマージのたびに回り、記録が残る。宛先の `SMOKE_BASE_URL` はすでに `staging` 環境の Secret にある。検証ゲートではないので `verify.yaml` を触らない | staging の DO に e2e のデータが溜まる（e2e 用のインスタンスを使う）。スモークの「10 分の線」とは分けて測る |
| B. 手元で回す | CI に負担をかけない | 記録が残らず、回し忘れる |

### おすすめと理由
- **おすすめ**：A
- **理由**：M1a のゲートの判定 5（staging の見本の SHA-256 の照合）を、機械で毎回確かめられるため

### 決定（2026-09-16 所有者）
- **おすすめ（A）で進める**
- 宛先は Secret から環境変数で渡し、`--base-url` を使わない。ログに URL を出さない規律（`CLAUDE.md`）はスモークと同じ

---

## Q17. 時刻で変わる値の採点で、時計をどう固定するか

### 何を決めるか
期限切れ・今月・月ごとなど、時刻で変わる値の採点（`02` の §3.3・§4.4。時計は 2026-09-15 に固定）を、どこでどう行うかを決める。

### なぜ今決める必要があるか
「今日」は data-api（サーバ側）で計算する（README §3.1）。staging に対して時計を固定するには、ログインの無い API に時刻を書き換える入口が要る。
M1a のゲートの判定 1 は「e2e テストで流す（時計を固定する）」と書いてあり、どこで流すかが決まっていなかった。割り勘は時刻に依存しないので M1.2 までは値に影響しないが、data-api を時計を差し込める作りにするかは M1.1 で決まる。

### 選択肢
| 選択肢 | 良い点（メリット） | 気になる点（デメリット・課題） |
|---|---|---|
| **A. 時計を差し込んだテスト（vitest）で採点する。staging の e2e は、時計に依存しない値と SHA の照合だけにする**（おすすめ） | ログインの無い API に時刻を変える入口を作らない。採点が CI（unit ゲート）で毎回回る | 期限切れ・今月の値は、staging では機械で確かめない。判定 2（スマホ）で確かめる |
| B. production 以外に限り、data-api が時計の上書きを受け付ける | 全部のシナリオを staging の本物の経路で採点できる | ログインの無い API に時刻を変える入口ができる。production で確実に閉じるテストが要る |
| C. シナリオの日付を「実行した日から何日前か」で書く | 時計の固定が要らない | `02` の期待値を書き直すことになる。月をまたぐと「今月」の値がぶれる |

### おすすめと理由
- **おすすめ**：A
- **理由**：守りに穴を作らずに、採点を機械で毎回回せるため。staging の経路そのものは、時計に依存しない値と判定 2・3 で確かめられる

### 決定（2026-09-16 所有者）
- **おすすめ（A）で進める**
- M1a のゲートの判定 1・2 を、これに合わせて直した（`01` §5.3。ゲートの変更の記録）
- spec-engine と data-api は、**M1.1 から時計を引数で受け取る作り**にする。API に時計を書き換える入口は作らない

---

## Q18. M1.1・M1.2 の契約の決めごと

### 何を決めるか
Issue の本文を実装できる水準へ精錬したときに出た、**契約として決めないと実装できない 8 点**を決める（2026-09-16。Codex に本文を精錬させて洗い出した）。

### なぜ今決める必要があるか
どれも**利用者の画面の値か、宣言の書き方そのもの**に効く。ワーカーが Issue 単位で並列に実装するので、先に決めておかないと実装のあいだで食い違う。

### 決定（2026-09-16 所有者。すべておすすめを採用）

| # | 決めごと | 決定 | 効く先 |
|---|---|---|---|
| 1 | 式の深さ・大きさの上限 | **深さ 8・AST ノード 64・式の文字数 200。** ちょうどは通し、1 超過で診断を返す。#111 の実測を見て M1.5 で見直す | #97・#98 |
| 2 | 参照 list の空と重複 | **型で断る**（`04` §1.1 の「空と重複を許さない」を維持）。画面の「1 人以上選んでください」は入力補助であって守りではない | #106 |
| 3 | 集計元に無効な値があるとき | **その集計値も null にする。** 空集合の 0 とは区別し、黙って 0 にしない。画面は null を「—」で見せる | #107 |
| 4 | `settle` の置き場所 | **ロジック層の関数として宣言し、view は結果（送金の配列）を参照するだけ**（`03` §2.3） | #108 |
| 5 | 余りの配賦の語彙 | **足さない。** `owed` は基準額の集計のままにし、余りは `settle` の内部規約（Q13）で解く（`04` §6.1） | #108 |
| 6 | 金額の型 | **割り勘の見本は整数円に限定**（`amount`・`discount` に整数の検査）。型としての `number` は小数を許したまま | #108 |
| 7 | 精算の組み方 | **一組を処理するたびに残額で並べ直す**（同額はメンバーの登録順）。Q13 の「大きい順」を毎回適用する | #108 |
| 8 | `update` の方式 | **全項目の置換**（`id`・`createdAt` は保持。欠けた項目は未入力として検査にかける） | #109 |

### あわせて決めた運用（2026-09-16 所有者）

| # | 決めごと | 決定 | 効く先 |
|---|---|---|---|
| 9 | staging の見本を誰が配るか | **CI が配る。** `deploy-staging` の貫通スモークのあとに publish を実行し、固定 ID の e2e 専用インスタンスへ置く。SHA が変われば同じインスタンスの登録を更新する（Q6 の積み残しをここで決めた） | #110 |
| 10 | 静的チェックの上限値をいつ確定するか | **M1.2 では確定しない。** 実測から根拠つきの提案までを出し、**確定は M1.5**（実測だけでは entity・集計・式の数の閾値を導けないため。`03` §6） | #111・M1.5 |

- 1〜8 は、実行時の意味の文書（`packages/appspec-schema/docs/semantics.md`）にも、実装する Issue の中で書き足す
- 変えるときは、Q13 と同じく**所有者の承認を得て、ここと `01` §5.3 に記録する**（採点のシナリオの期待値に効くため）

---

## Q19. 式の文法を、どの粒度で書き、どこに置くか

### 何を決めるか
式の文法（字句・構文・優先順位）と、宣言の構造・規則を、**どの粒度で書き、どこに置くか**を決める（#168）。

### なぜ今決める必要があるか
式の文法は `packages/spec-engine/src/expression.ts` の実装にしか無く、文書が無かった。工場（CommandAgent）は `computed`・`validations`・`when` の式を書くので、文法を渡せなければ M1b は成り立たない。
M1a のゲートの判定 6 は「スキーマの正本の SHA-256 を `pins/` に記録する」を求めるが、ハッシュを取る対象のファイルがリポジトリに無かった（`06` §1.1）。

### 選択肢
| 選択肢 | 良い点（メリット） | 気になる点（デメリット・課題） |
|---|---|---|
| A. 散文で意味の文書に書き足す | すぐ書ける | 機械が読めない。優先順位や上限の数え方が読み手ごとにぶれる。ハッシュの対象が決まらない |
| **B. 構造は JSON Schema、式は EBNF、規則は表で書き、1 つの版付きの契約に束ねる**（おすすめ） | 機械が読める。規則ごとに正例・負例・境界を書けるので、実装と照合できる。契約全体をハッシュの対象にできる | 書く量が多い。実装と食い違ったときにどちらを正とするかを決める手順が要る |
| C. B に加えて、文法から判定器を生成する | 実装と文法が必ず一致する | 本番の判定器が 2 つになる。M1.5 では過大（`06` §2.2） |

### おすすめと理由
- **おすすめ**：B
- **理由**：AI と工場に渡せる粒度で、実装との照合もでき、ハッシュの対象が 1 つに決まるため。判定器は増やさず、照合テストで食い違いを止める

### 決定（2026-09-24 所有者）
- **おすすめ（B）で進める**
- 置き場所は **`packages/appspec-schema/contract/`**。構造は `app-spec.schema.json`（#211）、式は `expression-grammar.md`（EBNF。#212）、規則は `rules.md`（#212）。英語でよい
- 照合は `packages/spec-engine/src/contract-rules.test.ts` と契約の SHA-256（#213）。**規則と実装が食い違ったら確定を止め、窓口が決める**
- `pins/commandagent.json` の `appspec_schema` は、**契約全体の SHA-256** を指す（#217。2026-09-24 に v0.2 として確定）
