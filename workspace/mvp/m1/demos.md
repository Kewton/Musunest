# デモと振り返り（マイルストーンごと）

> ロードマップの決まり（[`../roadmap.md`](../roadmap.md) §1）：**1 マイルストーンが終わったら、スマホで動かして見せ、何がうまくいき何を学んだかを 5 行で残す。**
> 実機での確認は人が行う（🧑 の Issue）。機械で確かめられる分は、その前に CI と e2e が見ている。
> **staging のオリジンは書かない**（`staging` 環境の Secret `SMOKE_BASE_URL`。`CLAUDE.md`「このリポジトリは public である」）。

---

## M1.1 宣言からフォームと一覧が出る（2026-09-17 合格）

| 項目 | 内容 |
|---|---|
| 着手日 | 2026-09-16（Issue #96〜#105 を起票した日） |
| 完了日 | **2026-09-17** |
| 実装の Issue | #96・#97・#98・#99・#100・#101・#102・#103・#104（9 本すべて merge 済み） |
| デモの Issue | #105（この記録） |
| 見たもの | 見本「支出の記録」（`packages/appspec-schema/samples/expense-log/`）。版 `community.app-spec/v0.2-draft`、原本 SHA `241be2dc…` |
| インスタンス | `m11-demo-expense-log`（staging。`publish` で配置） |
| 端末 | **Android / Chrome**（所有者のスマートフォン） |

### 確かめたこと

| # | 操作 | 結果 |
|---|---|---|
| 1 | staging の `/apps/m11-demo-expense-log` を開く | フォーム 5 項目（description・amount・discount・payer・participants）と「まだ記録がありません」が出た。360px 幅で崩れない |
| 2 | 夕食 6,600 円・割引 600 円・払った人 A・割る人 A/B/C | 一覧に出て、**1 人あたり 2,000 円** |
| 3 | タクシー 3,000 円・割引 0・払った人 B・割る人 A/B/C | **1 人あたり 1,000 円** |
| 4 | 金額 0 で追加 | 断られ、検査名が画面に出る（保存されない） |
| 5 | 再読み込み・PC の別ブラウザ | **2 件とも残っている**（DO に保存されている） |

### 機械で採った証跡（2026-09-17）

staging の host に対する読み取り（URL は出さない）。

```
GET /healthz                                        → 200
GET /api/instances/m11-demo-expense-log/spec        → 200  schemaVersion=community.app-spec/v0.2-draft
GET /api/instances/m11-demo-expense-log/views/expenseList → 200  rows=2
  夕食     amount=6600 discount=600 payer=A participants=[A,B,C]  paidAmount=6000 headcount=3 shareAmount=2000
  タクシー amount=3000 discount=0   payer=B participants=[A,B,C]  paidAmount=3000 headcount=3 shareAmount=1000
```

ブラウザ → host → gateway → data-api → R2 / D1 / DO が staging で通ったことの証跡でもある（#103 の完了条件から引き継いだ宿題）。

### 振り返り（5 行）

1. **宣言 1 枚から、画面・検査・計算・保存まで通った。** 手で書いた YAML が、そのままスマホで動くアプリになった
2. **契約を先に固める順番が効いた。** #96 で型・見本・負例・意味・台帳を揃えたので、後続の 8 本は「その上に積む」だけで済み、解釈の食い違いが出なかった
3. **並列開発の詰まりは、ツールではなく Issue の書き方だった。** `## 対象ファイル` の具体名・素の依存・地の文にパスを書かないという形を守れば、plan は通り dispatch は止まらない
4. **人の手が要ったのは 3 か所**：auto-yes の入れ直し、merge の承認（運用文書）、実機のスマホ確認。ほかは窓口 → 管理 → ワーカーで回った
5. **次に効きそうなこと**：BEHIND の往復は「逐次で回す」か「先に文書 PR を merge する」で減らせる。語彙が増える M1.2 は、1 Issue に語彙 1〜2 個の粒度を守る

### 残っていること（M1.2 以降へ）

- 表示名（`label`）とアプリ名は M1.2（#106）で入る。いまは項目名が英語のまま、タイトルはインスタンス ID
- 採点のシナリオの e2e を staging で回す仕組みは #110（`deploy-staging` の貫通スモークのあと）
- `/api` の CPU 時間の実測と P-7 の判定は #111

---

## M1.2 割り勘の精算が出る（2026-09-18 合格）

| 項目 | 内容 |
|---|---|
| 着手日 | 2026-09-16（Issue #106〜#112 を起票した日） |
| 完了日 | **2026-09-18** |
| 実装の Issue | #106・#107・#108・#142・#109・#145・#110・#111（8 本すべて merge 済み） |
| 途中で足した Issue | **#145**（SDK が `settle` を弾いていた。下の「学び」を見る）・**#150**（Workers Paid への昇格）・**#152**（10 ms の壁の検証） |
| デモの Issue | #112（この記録） |
| 見たもの | 見本「割り勘」（`packages/appspec-schema/samples/warikan/`）。版 `community.app-spec/v0.2-draft`、原本 SHA `68131ce1…` |
| インスタンス | `m12-demo-warikan`（staging） |
| コミット | `c7258b6` |
| 端末 | **Android / Chrome**（所有者のスマートフォン）＋ PC のブラウザ（窓口） |
| 機械で読める証跡 | [`demo-m1.2.json`](./demo-m1.2.json) |

### 確かめたこと

入力は **A・B・C を登録順に**、**夕食 6,000 円（A が払い A/B/C で割る）**、**タクシー 3,000 円（B が払い A/B/C で割る）**。

| view | 見えたもの |
|---|---|
| `memberList` | A `paid 6000 / owed 3000 / balance 3000`、B `3000 / 3000 / 0`、C `0 / 3000 / -3000` |
| `expenseList` | 夕食 `shareAmount 2000 / amount 6000 / payer A`、タクシー `1000 / 3000 / B` |
| `settlement` | **「C さん → A さん 3,000 円」の 1 件だけ** |

**3 観測（初回・再読込・別端末）で、すべて同じ値だった。**

| # | 観測 | 誰が | 結果 |
|---|---|---|---|
| 1 | 初回表示 | 窓口（PC のブラウザ） | 期待値どおり |
| 2 | 再読み込み | 窓口（同じブラウザ） | 期待値どおり |
| 3 | **別端末** | **所有者（Android / Chrome）** | **期待値どおり。画面の崩れの指摘なし** |
| 補助 | ブラウザを介さない読み取り | 窓口（手元のプロセスから `/api`） | 期待値どおり |

> **永続化は、要求より強く確かめられた。** データの `createdAt` は **2026-09-17 12:41 UTC** である。
> つまりこの 2 件は、**前日に入れたものが、そのあとの 6 回の配備（#146〜#153）を挟んでそのまま残っている**。
> 「再読み込みで残っている」より強い証拠になった。

### 昇格トリガーの扱い（#112 の完了条件）

| | 結果 | 対応 |
|---|---|---|
| **P-7** | **触れた**（#111・staging の `/api` で data-api 単体 **60.60 ms**） | **2026-09-18、所有者が宣言どおりアカウント①を Workers Paid へ上げた**（#150・PR #151。`../m0/06-plan-and-limits.md` §5） |
| **P-1** | 触れていない（7 日で `exceededResources` 0 件） | — |

**判断不能のまま完了にしていない。** 後継の線 **D-1（200 ms。触れてもプランは上げず作りに手を入れる）** と **P-8（production は `/api` を出す前に無条件で上げる）** も同じ日に決めた。

### 振り返り（5 行）

1. **9 つのゲートが全部緑でも、画面は真っ白になりうる。** SDK の型チェックが新語彙 `settle` を弾いていた（#145）。ユニットが「手書きの応答」しか見ておらず、**見本の正規化 JSON を通していなかった**——機械のゲートは「書いた形」しか見ない
2. **だから見本を通す回帰テストを足した。** `samples/` をディスクから列挙して全部 SDK に通す。**語彙を足すたびに自動で対象が増える**ので、同じ穴は二度と開かない
3. **Issue の分割は「層の線」で引くとうまくいく。** 35 ファイルに膨れた #108 を「計算はロジック層・見せ方は UI 層」で割ったら（#108 + #142）、片方は `sdk` と `host` に一切触らず終わった
4. **無償枠の壁は、品質の見張り番も兼ねていた。** $5 払って壁が消えた結果、**遅くしても壊れなくなった**。代わりの線（D-1）と「先に安い手を試す」を同時に決めないと、静かに遅くなる
5. **人の手が要ったのは 4 か所**：auto-yes の入れ直し、運用文書 PR の merge、実機のスマホ確認、**課金の判断**。ほかは窓口 → 管理 → ワーカーで回った

### M1.3〜M1.5 の残りの量の見直し

| | 残っていること | 量の見積もり | 順番 | 理由 |
|---|---|---|---|---|
| **M1.3**<br>タスク管理 | 選択肢（`enum`）と既定値、日付（`date`）と `today()`、決まった値への書き換え（`set`）とボタンを出す条件（`when`）、ボード（`board`）と強調（`highlight`）、絞り込み（`filters`）、見本の採点と staging の e2e | **Issue 7 本**（M1.2 と同じ規模）。語彙は 8 個で M1.2 の 6 個より多いが、**基盤（参照・集計・表）が M1.2 で入ったので 1 本あたりは軽い** | **変えない** | 時計（`today()`）の扱いは Q17 で決着済み（テストは差し込み、staging は時計に依らない値だけ見る）。M1.2 の積み残しは無い |
| **M1.4**<br>ダッシュボード | アプリ全体の集計、月ごと・種類ごと、平均、数値・棒・円・順位の部品 | **増やす**。UI の部品が 4 種類あり、`04-spec-evolution.md` §7.3 で**部品ごとに「空・多い・エラー・権限なし」の 4 状態**を決めた。M1.2 の実績（表 1 種類で 1 Issue）から見て **Issue 6〜8 本** | 変えない | — |
| **M1.5**<br>契約を固める | 静的チェックの残り（層の向き・守りの置き場所・画面の参照・**上限値**）、スキーマ v0.2 の確定、実行時の意味の文書、`pins/` の差し替え、テンプレート（#38） | **増やす**。**上限値の決定が M1.2 から回ってきた**（`measurements.md` §6 に根拠つきの提案がある。Q18-10）。さらに **D-1 に触れたときの手（行数の上限・ページング・先に計算しておく）もここに集まる** | 変えない | M1a の最終ゲートはここ。**M1.2 の完了は M1a の合格ではない** |

**ロードマップの変更は起案しない。** 3 つとも順番はそのままで、量の増減が M1.4・M1.5 に寄るだけである。

### 次に切った Issue（M1.3）

「今」と「次」だけ計画する決まり（[`../roadmap.md`](../roadmap.md) §3）に従い、**M1.3 の分だけ**起票した。番号は下の表のとおり。

| # | Issue | 層 | 依存 |
|---|---|---|---|
| 1 | **#154** 語彙：選択肢（`enum`）と既定値（`default`） | データ | なし |
| 2 | **#155** 語彙：日付（`date`）と「今日」（`today()`） | データ＋ロジック | なし |
| 3 | **#156** 語彙：決まった値への書き換え（`set`）とボタンを出す条件（`when`） | ロジック | #154 |
| 4 | **#157** UI：ボード（`board`）と強調（`highlight`） | UI | #154 |
| 5 | **#158** UI：一覧（`list`）と絞り込み（`filters`） | UI | #154 |
| 6 | **#159** 見本「タスク管理」と採点のシナリオ、staging の e2e | 見本 | #154・#155・#156・#157・#158 |
| 7 | **#160** 🧑 M1.3 のデモと、M1.4 への引き継ぎ（`human-only`） | 人 | #159 |

- **#154 が全部の土台**である（`when`・`board`・`filters` がすべて `enum` の上に乗る）。ここだけ先に通し、あとの 4 本は並列に回せる
- **層の線で割った**（M1.2 で #108 を #108 + #142 に割ったときと同じ考え方）。#157 と #158 は `spec-engine` の判定と `host` の部品だけで、`data-api` に触らない
- **#159 を独立させたのは #145 の反省**である。見本を最後に必ず通す Issue が無いと、「ゲートは緑だが画面は真っ白」がまた起きる


### 残っていること（M1.3 以降へ）

- **静的チェックの上限値は未確定**（M1.5。`measurements.md` §6 に提案がある）
- **F-1（10 ms を超えても `exceededResources` が 0 件）は未確定**（#152。実務上のリスクは P-8 で塞いである）
- `deploy-staging` の ⑤publish は、**見本の SHA が変わると既存インスタンスを指し直さない**（#110 の積み残し。見本を書き換える M1.3 で顕在化しうる）
- **#118**（worktree-setup の provider をリポジトリに持つ）は未着手。いまは手で roster をピン留めしている

---

## M1.3 タスク管理が動く（2026-09-19 合格）

| | |
|---|---|
| 見本 | `packages/appspec-schema/samples/task-board/app.spec.yaml` |
| 置いた場所 | staging（アカウント①）の **`/apps/m12-e2e-task-board`** |
| ⚠️ | **e2e 専用のインスタンスを使ってしまった。** `deploy-staging` が毎回 publish する固定 ID で、**e2e は後片付けでタスクを全部消す**（`e2e/src/task-board.ts`）。**このデモのデータは次の deploy で消える。** M1.2 はデモ専用の `m12-demo-warikan` を使っており、M1.3 も専用のインスタンスを置くべきだった——**窓口の段取り漏れである** |
| commit | `e9e82a3` |
| 確かめた人 | 所有者（スマホ）。窓口が同日に聞き取って記録した |
| 機械が読める証跡 | [`demo-m1.3.json`](./demo-m1.3.json) |

### 確かめたこと

| | ロードマップの文言 | 結果 |
|---|---|---|
| 1 | 期限切れのタスクが**赤くなり** | **部分的に合格**。強調の印（`▲ overdue`）は出たが、**赤くならない** |
| 2 | 「完了にする」が未完了の行にだけ出る | ✅ 出た |
| 3 | 押すと**列が移る** | ✅ 「完了」の列へ移った |
| 4 | 再読み込みしても残る | ✅ 残っていた |

**1 番が、M1.3 でいちばん確かめたかったところである。** staging の e2e は**時計に依らない値だけ**を見るので、
`overdue`（`due < today()`）が**本物の Cloudflare 上で、本物の今日の日付で**正しく出ることは、
このデモまで誰も見ていなかった。**通った。**

繋がったのはこの経路である。

1. 宣言の `computed`（`type: boolean`・`expression: due < today()`）
2. **Data API が JST で評価し、`ApiRow.computed.overdue` に真偽を載せる**
3. **画面は式を評価せず**、`highlight` が指す名前の値を見るだけ（`CLAUDE.md` の不変条件どおり）

### 赤くならない理由（仕様どおりである）

`apps/host/src/app/board.tsx` は**色を 1 色も使っていない**（`#c9c9c9` と `#4a4a4a` だけ）。
強調は**二重の枠線**と **`▲ overdue` の印**である。コードにも理由が書いてある。

> **色だけに頼らない印**（記号と、強調の名前）。色覚に依らずに分かる（`04` §7.2）

`04-spec-evolution.md` §7.2 の「強調は色だけに頼らない」を、**#157 の Issue で窓口が
「色が分からなくても分かる印を付ける」と書いた結果、色そのものが無くなった**。

**「色だけに頼らない」は本来「色 ＋ もう 1 つの手がかり」である。** いまは後者だけになっている。
そして**ロードマップ §4 の M1.3 の行は「赤くなり」と書いている**——文言と実装が食い違っている。

### 見つかったこと（M1.4 へ）

| # | 重要度 | 何が | どうする |
|---|---|---|---|
| **F-A** | 中 | 強調に**色が無い** | 二重枠と `▲` は残したまま、**赤系を足す** |
| **F-B** | 中 | 強調の印に **`overdue` と計算の名前がそのまま出る** | **`label` を足す**。項目名は `^[A-Za-z][A-Za-z0-9]*$` に限られるので、日本語を出す手段がこれしか無い |

**このデモが `label` の必要性を実物で裏づけた。** 画面に出ている英語は、項目名（`title`・`assignee`・`due`・`memo`）だけでなく、
**強調の印そのもの**でもあった。

> ただし「画面に日本語が出ない」は言い過ぎである。**`enum` の表示名（未着手・進行中・完了）と検査の文言は日本語で出る**
> （2026-09-19 の Codex のレビューで窓口の記述の誤りとして指摘された）。足りないのは
> **「識別子とは別の日本語の名前を付けること」**である。

### 確かめていないこと

- **OS とブラウザの版**（控えていない。M1.2 と同じ欠落。**M1a のゲート判定 2 が求めているので、M1a までに直す**）
- 別端末での表示（M1.2 では確かめたが、M1.3 では聞き取っていない）
- 横幅 360px での崩れ（単体テストでは見ているが、実機では確かめていない）

### 振り返り（5 行）

1. **Issue の `## 対象ファイル` の不足で 4 回止まった**（#154・#156 ×2・#157）。毎回「静的チェックは通るのに配信側・画面側が対応していない」形で、**原因はすべて窓口の書き漏れ**だった。ワーカーの落ち度は 1 件も無い
2. **止まったのは、管理がワーカーの「完了」を鵜呑みにしなかったからである。** #154 は 9 ゲート緑・`pnpm check` 緑で戻ってきたが、管理が自分でコードを読んで「`enum` の宣言は 503 になる」と見抜いた。**#145 と同じ形を、人の目で 1 回捕まえた**
3. **見本を `samples/` に置くと、既存の回帰が自動で効く。** #159 は `sdk-spec.test.ts` を**1 行も変えずに**テストが 2 件増えて緑になった。**語彙を足す Issue に見本を含めれば、同じ穴は機械が塞ぐ**（#167 に記録する）
4. **設計書が実装より先走っていた。** `02-l2-spec-examples.md` §3.2（2026-09-15 の叩き台）は、`label`・`required`・`and`・`not`・`card`・`sort`・`app` を使っていて**いまの語彙では書けなかった**。所有者は「設計書を直す」を選んだ（#169）。**叩き台がそう宣言していた**——「正式な形は、見本を実際に動かしながら決める」
5. **人の手が要ったのは 5 か所**：`#159` の判断（設計書か語彙か）、式に文字列の定数を足す判断、断りの誤りコードの判断、**実機のスマホ確認**、運用文書 PR の merge。加えて **CommandMate のサーバが 1 度落ち**、`commandmate send` が管理にだけ 3 回通らず、**tmux から直接送って回した**

### 残っていること（M1.4 以降へ）

- **F-A（色）と F-B（`label`）** — M1.4 で直す
- **`and`・`not`・`sort`・`card`・`required`** — 語彙を足す条件（`04` §6.1）に照らすと、いまは「見本が書けている」ので足さない側。**#168 の検証の結果が材料になる**
- **`deploy-staging` の ⑤publish は、見本の SHA が変わると既存インスタンスを指し直さない**（#110 の積み残し）。#159 では新規インスタンスだったので当たらなかったが、**次にどちらかの見本の SHA が変わると `instance_conflict` で落ちる**
- **静的チェックの上限値は未確定**（M1.5。`measurements.md` §6 に提案がある）
- **F-1（10 ms を超えても `exceededResources` が 0 件）は未確定**（#152。実務上のリスクは P-8 で塞いである）
- **#118**（worktree-setup の provider）は未着手
- **#167**（語彙を足すときに必ず触る場所・**dispatch の nudge がワーカーに「差分を進めてよい許可」と読まれた件**）・**#168**（文法の正本と AI 検証）・**#169**（設計書の修正）は窓口が書く

---

## M1.4 ダッシュボードが動く（2026-09-23 合格）

| | |
|---|---|
| 見本 | `packages/appspec-schema/samples/dashboard/app.spec.yaml`（原本 SHA `227d566b…`） |
| 置いた場所 | staging（アカウント①）の **`/apps/m14-demo-dashboard`**（**デモ専用**。`deploy-staging` はこの ID に触らない） |
| commit | `465ace8`（確認 8 の見直しの時点。デモの途中で見本を 2 回差し替えた（#200・#204 の merge 後。`--replace`）。下の「途中で止めたこと」） |
| 確かめた人 | 所有者（スマホ）。窓口が同日に聞き取って記録した |
| 端末 | **Android 16 / Chrome 153.0.8010.52**（**M1.1 から初めて版を控えた**。M1a のゲート判定 2 が求めている） |
| 機械が読める証跡 | [`demo-m1.4.json`](./demo-m1.4.json) |

### 確かめたこと

入力はすべて**画面から**行った（メンバー 4 人、活動 4 件。うち 1 件は**先月**の日付）。

| # | 確かめたこと | 期待 | 結果 |
|---|---|---|---|
| 0 | メンバーの一覧 | 4 人が並ぶ | ✅ |
| 1 | 最初の表示 | 0 回・0 人・**「—」**（0 と区別）、棒は 2026-04〜09 が 0 | ✅ |
| 2 | 今月の活動 | **3 回**（先月の 1 件は入らない） | ✅ |
| 3 | 今月の参加（のべ） | 6 人 | ✅ |
| 4 | 1 回あたりの参加 | 2 人 | ✅ |
| 5 | 今月の費用の平均 | 3333.3 円（小数第 1 位） | ✅ |
| 6 | 月ごとの棒 | 08 が 1・09 が 3 | ✅ |
| 7 | 種類の内訳（円） | 練習 2・試合 1・飲み会 1（50/25/25%） | ✅ |
| 8 | 参加の多い活動（順位） | 4 → 3 → 2 → 1、表示名つき | ⚠️ → ✅（**並びは初回から正しかったが、表示が識別子のままだった**。#204 で直して見直し） |
| 9 | 再読み込み | 値が残る | ✅ |

**2 と 6 が、M1.4 でいちばん確かめたかったところである。** 採点のシナリオも e2e も時計を固定するか、
時計に依らない値だけを見る。**「今月」の境目が、本物の Cloudflare 上で、本物の今日の日付（日本時間）で効くこと**は、
このデモまで誰も見ていなかった。**通った。**

### 途中で止めたこと（期待と違ったところ。丸めずに書く）

**1 回目：活動を 1 件も入力できなかった（確認 ③）。** 参加者の欄が「選べる候補がありません。先に登録してください。」になった。

- 画面は参照の候補を**参照先を一覧にする view の行**から読む。見本に member の一覧が無かった
- **7 段のゲートも staging の e2e も緑だった。** e2e は API で直接登録するので、画面のフォームを通っていない
- デモを止め、#200（見本に一覧を足す）・#201（静的チェックで落とす）・#202（運用文書）を切って直してから再開した

**2 回目：順位の部品の表示（確認 8）。** 並びは正しかったが、次のように見えた。

```
1
date2026-08-30
kindpractice
attendeeCount4
```

| | 見えたもの | 原因 |
|---|---|---|
| a | 見出しが識別子 | 一覧の応答の表示名（`labels`）は一覧の `entity` の分しか載らず、**ダッシュボードは `entity` を持たない**。見本も項目に表示名を書いていなかった |
| b | 選択肢がキー（`practice`） | **順位の部品だけ**が、一覧・ボード・円と共通の「選択肢を表示名に写す処理」を通っていなかった |
| c | 見出しと値がくっつく | 部品の見た目で区切りが抜けていた |

#204 で、画面が手元の宣言から表示名を引き、共通の処理を通すように直した（**応答の形は変えていない**）。

### 真因と横展開（窓口の分析）

**1 回目の欠陥は、5 つの層で見逃しが重なった**（詳細は `docs/parallel-development.md` §7.4）。

| # | 層 | 何が抜けていたか |
|---|---|---|
| 1 | 設計 | `02-l2-spec-examples.md` §4.2 の画面が 2 つで、参加者を登録・選択する画面が無かった |
| 2 | 静的チェック | 画面の前提（view の `entity`）を満たさない宣言を落とさなかった |
| 3 | 機械の検査 | シナリオ・SDK の回帰・e2e は**すべて API で登録する**。人が画面だけで使えるかを見る検査が無かった |
| 4 | 監督 | #183 のワーカーが PR に「一覧が無いので API で片付ける」と書き、管理も窓口も通した。**窓口はデモの準備でメンバーを足す画面が無いと気づきながら、API で種を入れて先へ進めた** |
| 5 | デモ | M1.3 までは、データの入ったインスタンスで見せていた。**空のインスタンスから画面だけで入れたのは M1.4 が初めてだった** |

- **横展開**：見本 4 つを走査した。画面から届かない参照・操作は **dashboard だけ**（参照 1・操作 2）だった。
  画面の部品を走査した。共通の見せ方を通っていないのは**順位の部品だけ**だった
- **再発防止**：静的チェック `UI_REF_TARGET_NOT_SHOWN`・`UI_ACTION_NOT_REACHABLE`（#201）／**迂回の自己申告は止める合図**／
  **デモのインスタンスは空で置き、画面からだけ入れる**（#202）／行を並べる部品は既存の見せ方の処理を使う（#204）

**直す途中で、窓口の `## 対象ファイル` の書き漏れで 2 回止まった**（#201 の 1 回目・#204 の 1 回目）。どちらも、
変更がテストの中の宣言（fixture）や、見本を写すテストの期待値に当たった。**2 回ともワーカーが自分で止めて返した。**
窓口が変更を手元で試して全テストを流し、当たるファイルを確かめてから scope に足して再投入した。

### 遅さの線（D-1）

- staging の data-api の Worker 単体の CPU（2026-09-23 の 1 日・363 リクエスト・Cloudflare の `workersInvocationsAdaptive`）：
  **最大 20.4 ms**・p99 11.7 ms・p50 3.6 ms。**D-1（200 ms）の 1/10 である**
- **ただし D-1 の測り方（200 件）ではない。** デモのデータは数件である。200 件での一覧の計算の回数は、#183 の単体テストが
  「行数に対して線形を超えない」ことを確かめている（CPU の実測は 200 件では行っていない）

### 確かめていないこと

- **200 件での CPU の実測**（上の D-1）
- 別端末での表示（M1.2 では確かめた）
- 10 月に入ったあとの「今月」（時計の月替わり。採点のシナリオは時計を固定して来月の行が入らないことを確かめている）
- 順位の部品に参照の項目を出したときの表示（**ID のまま出る**。見本は参照を `show` に入れていない。#204 の部品の冒頭に記録）

### 振り返り（5 行）

1. **緑のゲートは「書いた経路」しか見ない。** 7 段・e2e・SDK の回帰がすべて緑でも、画面から 1 件も入力できなかった。**M1.2 の振り返り 1 と同じ形を、1 つ上の層（画面）で踏んだ**
2. **兆候は 3 回見えていた。** ワーカーの「一覧が無いので API で」、管理の「指示どおり」、窓口の「API で種を入れる」。**迂回を申告してくれたのは良いことで、見送ったのが穴だった**
3. **空のインスタンスから、画面だけで入れるデモが効いた。** 「今月」の境目が本物の日付で効くことも、画面から届かない宣言も、これで見つかった。**種を入れたくなったこと自体が欠陥の合図である**
4. **ワーカーが止まってくれたおかげで、窓口の書き漏れが 2 回とも表に出た。** 読み替えて完遂していれば、テストを弱める変更が通っていた。**止める条件は字面ではなく意味で書く**（#201 の 2 回目は字面で止まった）
5. **人の手が要ったのは 7 か所**：スマホの確認（2 回）、#201・#204 の scope の裁定、#201 の申告 2 点の承認、運用文書 PR の merge（#198・#206）、worktree の片付けの承認。**auto-yes は 1 度期限で切れて入れ直した**

### 残っていること（M1.5 以降へ）

- **#168**（文法の正本と AI が宣言を書けるかの検証）——M1.5 の中心。**第 0 段（疎通確認）も未実行**
- **静的チェックの上限値**（`measurements.md` §6 の提案）と、**D-1 に触れたときの手**（行数の上限・ページング・先に計算しておく）
- **「画面の参照」の静的チェックの残り**——#201 で「届くか」は入った。部品が指す名前の検査などが残る
- 順位の部品の**参照の項目**（ID のまま出る）
- **#201 の規則は、画面が実際に出すボタンより緩い。** 一覧の種類 `list` は削除のボタンを出さない（削除のボタンを出すのは `table` だけ）。見本 dashboard の `deleteMember`・`deleteActivity` は e2e の片付けのための操作で、**`list` の一覧しか無いので画面からは押せない**が、規則は「その entity の view がある」ことしか見ないので落とさない。**操作の種類と view の種類の組まで見るかを M1.5 で決める**（記録を書くときに窓口が見つけた）
- **#118**（worktree の provider）——M1.4 で**実害 2 回**（#178 が Claude に渡る／新しい worktree の登録に `sync` が要る）
- worktree `Musunest-issue-179`・`-181` が残っている（merge の証明が規則を満たさないので自動では消していない。人が消す）

---

## M1.5 契約を固めて、工場に渡せる（2026-09-26 合格）

| | |
|---|---|
| 置いた場所 | staging（アカウント①）の**デモ専用**インスタンス `m15-demo-warikan`・`m15-demo-task-board`・`m15-demo-dashboard`（版 `community.app-spec/v0.2`。**空で置き、データは画面からだけ入れた**。手順書 §7.4） |
| commit | `9a7013a`（M1.5 の実装の最後。テンプレートのピン #232） |
| 確かめた人 | 所有者（スマホ）。窓口が同日に聞き取って記録した |
| 端末 | **Android 16 / Chrome 153.0.8010.52**（M1.4 と同じ） |
| 機械が読める証跡 | [`demo-m1.5.json`](./demo-m1.5.json) |

### M1.5 で入ったもの

| | Issue |
|---|---|
| 負例の穴埋め（負例の無い 20 コード。負例は 81 本に） | #210 |
| 文法の正本：宣言の構造（JSON Schema）・式の文法（EBNF）と規則の表・照合テストと契約の SHA-256 | #211・#212・#213 |
| 画面：table・list・board のどれでも消すボタン、`set` を持たない書き換えの「直す」フォーム（M1.2 で約束していた「直す」が初めて画面に出た） | #214・#215 |
| 200 件の CPU の実測（D-1 に触れていない。最大 55.44 ms） | #216 |
| **スキーマ v0.2 の確定**（`v0.2-draft` を外し、`pins/` のスキーマのピンを契約の SHA-256 に同じコミットで差し替えた。所有者の承認 Q9） | #217 |
| テンプレートの境界と再現できる tarball、GitHub Releases、`pins/` のテンプレートのピン | #229・#38 |
| worktree の provider（ワーカーの CLI を Command Code に固定） | #118 |
| 文法の明文化を M1.5 に入れ、Q19 を決めた／AI が文法の正本から宣言を書けるかの試行（第 0 段） | #168（#228・#230） |

### 確かめたこと（スマホ）

窓口が手順（割り勘・タスク管理・ダッシュボード・残るか）を案内し、所有者が流した。

- **結果：所有者の見立てで「ざっと見る限り OK」。** 項目ごとの合否は聞き取っていない（下の「丸めずに書くこと」）
- 案内した手順の要点：割り勘は「C さん → A さん 3,000 円」・直すで精算が変わる・消す・0 円は断られる／タスク管理は期限切れの強調・「始める」「完了にする」で列が移る・直すで強調が消える・消す／ダッシュボードは今日の活動で「今月の活動」が 0 → 1・消すと 0 に戻る／再読み込みと別の端末で残る

### 「わざと壊した宣言」のデモ（窓口が PC の端末で）

割り勘の見本の参照先を `menber` と打ち間違え、金額の検査を文字列と比べる形にして `spec:check` にかけた。**4 件の誤りが、行と列つきの日本語で出た。**

```
broken-warikan.yaml:40:13: DATA_REF_TARGET_NOT_FOUND: entity expense の項目 payer の参照先 entity menber が宣言に無い
broken-warikan.yaml:88:17: LOGIC_OPERAND_TYPE_MISMATCH: validation positiveAmount: > は文字列どうしを比べるに使う（左が数）
broken-warikan.yaml:116:9: LOGIC_AGGREGATE_WHERE_TYPE_MISMATCH: computed paid の集計の where の payer は、member を指す参照（ref）でなければならない
broken-warikan.yaml:145:14: LOGIC_SETTLE_REFERENCE_TYPE_MISMATCH: computed settlement の精算の払った人 expense.payer は、member を指す参照（ref）でなければならない
```

### M1a のゲートの判定（`01` §5.3）

| 判定 | 中身 | 結果 | 根拠 |
|---|---|---|---|
| 1 | 3 つの見本の採点のシナリオ（時計を差し込んだテスト）と、staging の e2e（時計に依らない値） | ✅ | `main`（`9a7013a`）の CI と `deploy-staging` が緑（⑥ e2e 3 本） |
| 2 | スマホ 1 台で、割り勘の精算・期限切れの強調と列の移動・今日の活動で「今月」が増える（OS とブラウザを記録） | ✅（所有者の目視で概ね OK） | 上の「確かめたこと」。端末は Android 16 / Chrome 153.0.8010.52 |
| 3 | 再読み込み・別の端末でもデータが残る | ✅（同上） | 同上 |
| 4 | 3 つの見本が新しい版の検査に通り、負例は落ちる | ✅ | CI 緑。負例 81 本 |
| 5 | staging の見本の SHA-256 がリポジトリと一致する | ✅ | `deploy-staging` の ⑥ e2e が照合 |
| 6 | スキーマの正本の SHA-256 が `pins/` に記録され、工場へ渡す値として確定している | ✅ | `pins/commandagent.json` の `appspec_schema` と `contract:hash` がどちらも `6078c388…a7379`（#217）。テンプレートのピンも実値（#38） |

**窓口の見立ては M1a 合格。判定者は所有者**（`01` §5.3）で、この PR の merge を判定とする。

### 丸めずに書くこと（期待と違った・確かめていない）

- **判定 2・3 は項目ごとには確かめていない。** 所有者の「ざっと見る限り OK」による。画面の操作を機械で流す画面テスト（#233・#234）は、M1.5 には入れず次から使う
- **誤りの文言に不自然な日本語がある**（「文字列どうしを比べるに使う」）
- **打ち間違い 1 つから誤りが 3 件に連鎖する**（参照先の `menber` が、集計と精算の検査にも波及する）。#201 は画面の検査だけ「ほかの誤りがあれば見ない」にしたが、ロジックの検査は連鎖を止めていない
- **窓口の Issue の書き漏れで 3 回止まった**（#211：地の文の `/` の無いファイル名が scope に入った／#212：planner が拡張子 `.ebnf` を拾わなかった／#213：規則の表の例 3 か所が表自身の決まりを満たしていなかった → #217 で直した）。どれもワーカーか管理が止めて返した
- **Issue の前提の誤り**：#215 に「SDK に直す口が無い」と書いたが、既存の `addRecord` が書き換えを受けられた。ワーカーは実際の姿を正とした
- 計測の道具の判定の線が Free の 7 ms（P-7）のまま（#216 の記録に書いた）
- 200 件より多いときの CPU の伸び方は測っていない

### 振り返り（5 行）

1. **契約が「実装の中」から「ファイル」になった。** 文法の正本（構造・式・規則）を書き、実装と規則単位で照合し、その SHA-256 を `pins/` に固定した。工場に渡すものが初めて 1 つの値で指せる
2. **照合テストは、実装ではなく文書の誤りを見つけた。** 規則の表の例 3 か所が表自身の決まりを破っていた。「期待値は実装から作らない」（`06` §2.2）が効いた
3. **止まる回数は減らなかったが、止まり方が良くなった。** 3 回とも窓口の書き漏れで、3 回ともワーカーが読み替えずに返した。**変更を手元で試して全テストを流してから scope を書く**（§7.4）を、見本の変更にも当てはめる
4. **「直す」が M1.2 から画面に出ていなかった。** 宣言も API も正しく、画面だけが足りなかった。M1.4 の「画面から入力できない」と同じ種類で、**画面テストの自動化（#233）**に進んだ
5. **人の手が要ったのは**：スキーマのピンの承認（Q9）、テンプレートの Release の承認、運用文書の PR の merge、スマホでの確認、#201・#204 系の scope の裁定。**窓口のセッションが落ちても再開できるよう、引き継ぎの仕組み（#235）を作った**

### 残っていること（M1.6 以降へ）

- **M1b（M1.6）**：工場（CommandAgent）が新しい契約で作ったアプリが、見本と同じ値を出す。工場側のピンの照合
- **#168 の第 1 段以降**（AI が文法の正本から宣言を書けるかの本番）。M1.6 の準備として進める
- 画面テスト（#233・#234）
- 小さな改善：誤りの文言・誤りの連鎖・計測の道具の判定の線・順位の部品の参照が ID のまま
