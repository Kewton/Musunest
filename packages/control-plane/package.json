{
  "name": "@musunest/control-plane",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Better Auth / User / Community / Membership / AppGrant（D1 専用）",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty",
    "lint": "oxlint src",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@musunest/appspec-schema": "workspace:*",
    "@musunest/spec-engine": "workspace:*"
  }
}
